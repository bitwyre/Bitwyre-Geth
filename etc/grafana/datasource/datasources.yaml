apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
      defaultBucket: my_bucket
      httpMode: "POST"
      organization: my_org
      version: "Flux"
    secureJsonData:
      token: my_token
