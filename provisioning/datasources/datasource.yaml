apiVersion: 1

datasources:
  - name: InfluxDBV2
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
      defaultBucket: my_bucket
      httpMode: "POST"
      organization: my_org
    secureJsonData:
      token: my_token
