apiVersion: 1

datasources:
  - name: InfluxDBV2
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
     version: SQL
      metadata:
        - database: my_bucket
    secureJsonData:
       httpHeaderValue1: 'Token my_token'
